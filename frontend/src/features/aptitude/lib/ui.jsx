import { Sparkles } from "lucide-react";
export function Card({ children, style, className = "", padding = "20px 22px" }) { return <div className={`cv-card ${className}`} style={{ padding, ...style }}>{children}</div>; }
export function SectionHeader({ icon, title, sub, action, onAction }) { return <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start", marginBottom: 16, gap: 12 }}><div style={{ display: "flex", alignItems: "center", gap: 10 }}>{icon}<div><h3 style={{ fontSize: 15, fontWeight: 800, color: "var(--text-primary)", margin: 0 }}>{title}</h3>{sub && <p style={{ fontSize: 12.5, color: "var(--text-tertiary)", margin: "3px 0 0" }}>{sub}</p>}</div></div>{action && <button onClick={onAction} className="cv-button-secondary" style={{ padding: "6px 12px", fontSize: 12 }}>{action}</button>}</div>; }
export function EmptyState({ icon = <Sparkles size={20} />, title, description, action, onAction }) { return <div style={{ textAlign: "center", padding: "34px 16px" }}><div style={{ width: 48, height: 48, borderRadius: 14, background: "var(--accent-soft)", color: "var(--accent-strong)", display: "flex", alignItems: "center", justifyContent: "center", margin: "0 auto 14px" }}>{icon}</div><div style={{ fontSize: 14, fontWeight: 700, color: "var(--text-primary)", marginBottom: 6 }}>{title}</div>{description && <p style={{ fontSize: 12.5, color: "var(--text-tertiary)", maxWidth: 340, margin: "0 auto 16px", lineHeight: 1.6 }}>{description}</p>}{action && <button onClick={onAction} className="cv-button-primary" style={{ padding: "8px 18px", fontSize: 12.5 }}>{action}</button>}</div>; }
export function ProgressBar({ value = 0, color = "var(--accent)" }) { return <div style={{ width: "100%", height: 6, borderRadius: 100, background: "var(--bg-elevated-2)", overflow: "hidden" }}><div style={{ width: `${Math.max(0, Math.min(100, value))}%`, height: "100%", background: color, borderRadius: 100 }} /></div>; }
export function Pill({ children, color = "var(--text-secondary)", bg = "var(--bg-elevated)" }) { return <span style={{ display: "inline-flex", alignItems: "center", gap: 5, fontSize: 11, fontWeight: 700, padding: "3px 9px", borderRadius: 100, color, background: bg }}>{children}</span>; }
export function PageLoading() { return <div className="cv-aptitude-skeleton" style={{ height: 180, borderRadius: 16 }} />; }
export function ErrorNote({ message, onRetry }) { return <Card style={{ borderColor: "var(--red)", textAlign: "center" }}><p style={{ color: "var(--red)" }}>{message}</p>{onRetry && <button onClick={onRetry} className="cv-button-secondary">Try again</button>}</Card>; }
