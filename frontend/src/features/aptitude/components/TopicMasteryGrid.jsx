import { Layers } from "lucide-react";
import { Card, EmptyState, ProgressBar, SectionHeader } from "../lib/ui";
export default function TopicMasteryGrid({ topics = [] }) { const ready = topics.filter((t) => t.masteryScore !== null); return <Card><SectionHeader icon={<Layers size={16} />} title="Topic mastery" sub="Score appears after enough attempts" />{!ready.length ? <EmptyState icon={<Layers size={20} />} title="Build your baseline" description="Keep practicing to unlock topic mastery." /> : <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill,minmax(200px,1fr))", gap: 12 }}>{ready.map((t) => <div key={`${t.category}-${t.topic}`}><div style={{ display: "flex", justifyContent: "space-between" }}><strong>{t.topic}</strong><strong>{t.masteryScore}%</strong></div><ProgressBar value={t.masteryScore} /><small>{t.totalAttempts} attempts · {t.category}</small></div>)}</div>}</Card>; }
